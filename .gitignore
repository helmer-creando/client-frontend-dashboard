# ── macOS ──
.DS_Store
._*

# ── Editors / IDEs ──
.idea/
.vscode/
.claude/
*.swp
*.swo
*~

# ── OS junk ──
Thumbs.db
Desktop.ini

# ── Logs ──
*.log

# ── Design assets (Stitch 2.0) ──
stitch-design/

# ── Dependencies (future-proofing) ──
# Anchor to repo root so this only matches Composer's /vendor/,
# NOT bundled front-end libs in assets/js/vendor/ (e.g. iro.min.js).
node_modules/
/vendor/

# ACSS docs — symlinked dev reference, never ship to clients
docs/acss-ref
